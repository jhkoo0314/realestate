# 부동산 매물관리 도구 구현 작업표

> 작업 방식: 먼저 작동하는 뼈대를 만들고, 매 단계마다 실제 브라우저 화면을 보면서 수정한다.
>
> 기준 문서: `docs/` 폴더의 PRD, 데이터 항목 정의서, 화면 명세, 업무 흐름서, 테스트·운영 문서

## 0. 이 문서 사용법

- 한 번에 완성하려 하지 않는다. 아래 **단계 순서대로** 만든다.
- 각 작업을 마치면 브라우저에서 직접 확인한 뒤 체크박스를 표시한다.
- 화면을 보고 불편한 점이 생기면 바로 코드를 고치기보다, 먼저 이 문서의 `결정 기록`에 적는다.
- 기능을 바꿀 때는 관련된 `docs/` 문서와 테스트 항목도 함께 확인한다.
- 1차 버전의 목표는 “화려한 서비스”가 아니라 **매물을 빠르고 안전하게 등록·확인하는 내부 도구**다.

### 코드 파일과 폴더를 나누는 원칙

- `app.py`는 프로젝트 최상위 폴더에 두는 프로그램의 **입구**다. 상단 메뉴, 공통 화면 모양, 화면 전환만 관리한다.
- 사용자에게 보이는 화면은 `ui/` 폴더에서 관리한다.
- 신규 등록·재등록·수정의 입력 검사와 업무 규칙은 `services/` 폴더에서 관리한다.
- 데이터 파일을 읽고 저장하는 코드는 `storage/` 폴더에서 관리한다.
- 새 기능이 생길 때는 역할에 맞는 기존 폴더에 파일을 추가한다. 루트 폴더에 기능 파일을 계속 쌓지 않는다.
- 공통 선택값, 입력 검사, 화면 문구처럼 여러 기능이 함께 쓰는 내용도 필요해질 때 역할별 폴더에 공통 파일로 관리한다.
- 처음부터 불필요하게 많은 파일을 만들지는 않는다. 기능이 실제로 생기는 단계에 맞춰 한 파일씩 추가한다.

현재 구조:

```text
C:\realestate\
├─ app.py                  # 실행과 메뉴 전환
├─ ui\                     # 사용자 화면
├─ services\               # 등록·수정 규칙
├─ storage\                # 데이터 저장과 실제 데이터 파일
└─ docs\                   # 기획·작업 문서
```

---

## 1. 완성 모습

사무실 직원이 브라우저에서 다음 3개 화면을 사용한다.

1. **매물 현황 리스트**: 현재 매물을 조회·필터하고 확인 업무를 찾으며, 조회 결과를 엑셀로 내보낸다.
2. **매물 등록·수정**: 새 건물·호실·매물을 등록하거나 기존 호실의 최신 정보를 수정·종료한다.
3. **건물·호실 관리**: 건물 공통정보, 호실 고정정보, 과거 매물 이력을 확인한다.

핵심 안전장치:

- 동일 건물·호실의 중복 등록 방지
- 기존 호실 최신 정보 수정 시 계약·상담 연결 유지
- 비밀번호·내부 메모의 기본 목록 노출 방지
- 저장 오류 시 입력한 내용 유지
- 원본 데이터와 백업 파일 분리

---

## 2. 1차 버전에서 만들 범위

### 반드시 만든다

- 건물, 호실, 매물 회차 저장
- 건물명·지번·호수 검색
- 신규 등록, 기존 건물의 새 호실 등록, 기존 호실 최신 정보 수정
- 현재 매물 조건 수정 및 매물 종료
- 오늘의 확인 업무(재확인·사진·현장·입주일·광고 준비)
- 목록 검색·필터·상세 보기
- 일반 매물 목록 엑셀 내보내기
- SQLite 데이터 저장과 백업
- 매물 회차에 연결한 단기·일반 계약 기간과 상태 관리
- 매물 회차에 연결한 간단한 상담 이력과 다음 연락 관리

### 이번에는 만들지 않는다

- 기존 `구재현` 시트 자동 가져오기
- 방문 예약, 자동 문자 발송, 주민번호 등 민감 개인정보, 계약금·잔금의 납부·결제 관리
- 외부 인터넷 공개, 로그인, 권한별 사용자 관리
- 사진 파일 업로드·사진 위치·사진 폴더 경로·링크 관리
- 여러 사람이 같은 매물을 동시에 수정하는 기능

---

## 3. 권장 개발 순서

## 단계 1 — 실행 가능한 빈 화면 만들기

목표: 프로그램을 실행하면 세 메뉴와 빈 화면이 보이게 한다.

- [x] Python `base` 환경과 필요한 패키지 목록을 준비한다.
- [x] `app.py`를 만들고 Streamlit 화면이 열리는지 확인한다.
- [x] 상단 메뉴에 아래 항목을 만든다.
  - [x] 매물 현황 리스트
  - [x] 매물 등록·수정
  - [x] 건물·호실 관리
- [x] 첫 화면은 `매물 현황 리스트`로 설정한다.
- [x] 데이터가 없을 때 “현재 운영할 첫 매물 등록” 버튼과 안내문을 보여 준다.

### 화면 확인

- [x] 프로그램 주소(`http://127.0.0.1:8501`)가 정상 응답한다.
- [x] 자동 테스트에서 첫 화면, 등록 버튼, 세 메뉴 이동을 확인한다.
- [ ] 실제 브라우저에서 첫 화면·메뉴 이동·등록 안내를 시각적으로 확인한다.

---

## 단계 2 — 데이터 저장 뼈대 만들기

목표: 프로그램을 껐다 켜도 등록한 정보가 남는다.

### 만들 데이터 묶음

- [x] **건물**: 건물명, 지번, 주소, 엘리베이터, 주차, 공동현관 비밀번호, 내부 메모 등
- [x] **호실**: 호수, 층, 룸 형태, 방향, 옵션, 방문 방법, 방문 비밀번호 등
- [x] **매물 회차**: 상태, 보증금, 월세, 관리비, 입주 가능일, 사진 보유 여부, 메모, 확인 예정일 등

### 저장 규칙

- [x] 건물 1개에는 여러 호실이 연결된다.
- [x] 호실 1개에는 여러 매물 회차가 연결된다.
- [x] 금액은 숫자로만 저장하고 화면에는 `만원` 단위를 표시한다.
- [x] 날짜·상태처럼 찾아야 하는 정보는 메모가 아닌 별도 칸에 저장한다.
- [x] 생성일과 수정일은 자동 기록한다.
- [x] 프로그램 시작 시 데이터 파일 경로가 없으면 빈 데이터 파일을 조용히 만들지 말고 안내한다.

### 화면 확인

- [x] 테스트용 건물·호실·매물 1건을 저장하고 연결 상태를 확인한다.
- [x] 테스트 데이터 파일을 다시 열어도 같은 건수와 이력이 남는지 확인한다.
- [x] 첫 화면에 데이터 파일 위치와 건물·호실·매물 건수를 표시한다.
- [ ] 실제 브라우저에서 데이터 파일 위치와 `0건` 표시를 확인한다.

---

## 단계 3 — 신규 등록 화면 만들기

목표: 새 건물, 새 호실, 첫 매물을 한 번에 안전하게 등록한다.

### 이 단계에서 만들 파일 역할

- [x] `app.py`: 등록 화면을 선택해 보여 주기만 한다.
- [x] `ui/listing_form.py`: 신규 등록 화면과 저장 전 확인 화면을 담당한다.
- [x] `services/listing_service.py`: 입력값 검사와 건물·호실·매물 저장 순서를 담당한다.
- [x] `storage/database.py`: `listing_service.py`가 요청한 저장·조회만 처리한다.

등록 화면이 커져도 `app.py`에 입력칸과 저장 규칙을 직접 추가하지 않는다.

### 기본 입력칸

- [x] 건물명(필수), 지번(필수)
- [x] 호수(필수), 룸 형태, 층, 방향
- [x] 매물 접수일(기본값 오늘, 실제 접수일로 수정 가능), 매물 상태(필수)
- [x] 보증금·월세(필수, 만원 단위 숫자)
- [x] 관리비, 입주 가능 유형, 입주 가능일
- [x] 사진 보유 여부, 이번 매물 메모
- [x] 호실 옵션: 냉장고, 세탁기, 에어컨, TV, 가스렌지, 인덕션, 옷장, 신발장 체크와 기타 옵션 메모

### 입력 검사

- [x] 건물명 또는 지번이 비면 저장하지 않는다.
- [x] 호수가 비면 저장하지 않는다.
- [x] 보증금·월세에는 숫자 입력칸을 사용하고, 비어 있거나 0이면 이유를 알려 준다.
- [x] 입주 가능 유형이 `날짜 지정`이면 날짜 입력을 요구한다.
- [x] 오류가 나도 이미 입력한 내용은 지우지 않는다.
- [x] 저장 전 `건물명 · 지번 · 호수 · 보증금/월세` 요약을 보여 준다.
- [x] 건물·호실·첫 매물은 모두 성공할 때만 한꺼번에 저장한다. 중간에 하나라도 실패하면 일부만 저장되면 안 된다.

### 화면 확인

- [x] 테스트용 `대성빌 / 북수리 1026 / 302호 / 500/45`를 별도 테스트 데이터 파일에 저장한다.
- [x] 저장한 건물·호실·매물 1건과 상세값을 다시 읽어 확인한다.
- [x] 필수값 누락과 날짜 지정 후 날짜 누락을 자동 시험한다.
- [ ] 실제 브라우저에서 신규 등록 화면의 입력 흐름과 저장 결과를 확인한다.

---

## 단계 4 — 검색·중복 방지 만들기

목표: 같은 건물이나 같은 호실을 잘못 여러 번 만들지 않는다.

- [x] 건물명 또는 지번으로 기존 건물을 찾는다.
- [x] `건물명 + 지번`이 같으면 기존 건물을 먼저 보여 준다.
- [x] 건물명은 같아도 지번이 다르면 자동으로 합치지 않는다.
- [x] 같은 건물에서 같은 호수를 새로 등록하지 못하게 한다.
- [x] `302`와 `302호`를 같은 호실로 처리한다.
- [x] 기존 건물을 선택하면 주소·공동현관·주차 같은 건물 정보는 다시 입력하지 않는다.

### 화면 확인

- [ ] 대성빌을 다시 검색했을 때 기존 건물이 우선 보인다.
- [ ] 대성빌에 302호를 다시 만들려고 하면 막힌다.
- [ ] 대성빌에 201호를 새로 등록할 수 있다.

---

## 단계 5 — 기존 호실 최신 정보 수정

목표: 기존 호실을 선택해 새로 확인한 조건만 입력하고 최신 매물 정보를 바로 갱신한다.

- [x] 건물과 호실을 선택할 수 있다.
- [x] 호실의 고정정보(구조·방향·옵션·방문 방법)를 자동으로 불러온다.
- [x] 기존 최신 조건을 불러와 필요한 값만 수정할 수 있다.
- [x] `최신 정보 저장` 버튼으로 같은 매물 기록을 갱신한다.
- [x] 저장 시 연결된 계약·상담 기록을 유지한다.
- [x] 기존 호실을 선택하면 최신 정보 입력칸을 바로 연다.
- [x] 실제로 다시 확인하지 않은 비밀번호·옵션의 마지막 확인일을 자동으로 오늘 날짜로 바꾸지 않는다.

### 화면 확인

- [ ] 대성빌 302호를 선택해 `500/47, 공실, 즉시입주`로 최신 정보를 저장한다.
- [ ] 연결된 계약·상담 기록이 그대로 남아 있는지 확인한다.

---

## 단계 6 — 현재 매물 수정과 종료 만들기

목표: 현재 매물의 변경과 매물 종료를 헷갈리지 않게 한다.

- [x] `현재 매물 수정`은 같은 매물 회차의 가격·상태·입주일·메모만 수정한다.
- [x] 여러 항목을 바꿔도 저장 버튼을 누를 때 한 번에 반영한다.
- [x] 사진 보유 여부를 바꾸면 사진 촬영·확인 필요 업무가 자동으로 갱신된다.
- [x] 종료 시 종료일과 종료 사유를 기록한다.
- [x] 계약 여부를 실제로 확인하기 전에는 자동으로 `계약 완료`로 바꾸지 않는다.
- [x] 잘못 등록한 매물은 `정보 오류`로 종료하거나, 확인 후 완전 삭제할 수 있다. 완전 삭제 시 연결된 계약·상담 기록도 함께 삭제한다.
- [x] 건물은 비활성화 방식으로 유지한다. 잘못 만든 호실은 삭제 대상 매물·계약·상담 건수를 확인한 뒤 완전 삭제할 수 있게 한다.

### 화면 확인

- [ ] 월세만 수정해도 새 매물 회차가 생기지 않는다.
- [ ] 계약 완료 처리한 매물은 현재 목록에서는 사라지고 이력에는 남는다.

---

## 단계 7 — 매물 현황 리스트 화면 만들기

목표: 현재 매물을 조회·필터하고, 필요한 확인 업무를 찾는다.

### 상단 요약

- [x] 오늘 새 접수
- [x] 퇴실 예정
- [x] 재확인 필요
- [x] 사진 촬영 필요
- [x] 공실

### 검색과 필터

- [x] 건물명, 지번, 호수 통합 검색
- [x] 매물 접수일 기간 필터: 시작일과 종료일을 선택해 그 기간에 접수된 매물만 찾기
- [x] 매물 상태 필터
- [x] 룸 형태 필터
- [x] 사진 보유 여부(있음 / 없음 / 확인 필요)를 선택하고 필터할 수 있다.
- [x] `현재 매물만 / 종료된 매물만 / 전체` 중 표시할 매물 범위를 선택할 수 있다.
- [x] 확인 업무 필터
- [x] 여러 필터를 동시에 적용
- [x] 필터 초기화

### 목록과 빠른 작업

- [x] 현재 매물 표에는 상태, 건물명, 호수, 룸 형태, 보증금, 월세, 관리비, 입주 가능, 사진 보유 여부, 재확인 예정일, 메모를 표시한다. 종료일·종료 사유는 종료된 매물 또는 전체 조회에서만 표시한다.
- [x] 비밀번호·내부 메모는 절대 기본 목록에 표시하지 않는다.
- [x] 행을 선택하면 같은 화면 아래에서 상세정보를 펼친다.
- [x] 상태, 사진 보유 여부, 청소·도배·수리 상태, 재확인 예정일 같은 자주 바꾸는 값은 빠르게 수정할 수 있게 한다.
- [x] 접수일 기간 필터가 적용된 상태를 화면에 분명히 표시한다. 날짜를 비우면 전체 기간을 조회한다.

### 자동으로 보여 줄 확인 업무

- [x] 재확인 예정일이 오늘 또는 지남 → `재확인 필요`
- [x] 사진 보유 여부가 없음 → `사진 촬영 필요`
- [x] 사진 보유 여부가 확인 필요 → `사진 확인 필요`
- [x] 청소·도배·수리가 필요 또는 진행 중 → `현장 상태 확인 필요`
- [x] 입주 가능 유형이 확인 필요 → `입주 가능일 확인 필요`
- [x] 매물 상태가 확인 필요 → `매물 상태 확인 필요`
- [x] `해야 할 일`은 별도 입력하지 않고, 매물 등록·최신정보 수정과 현황 리스트 빠른 수정에서 같은 관리 상태를 저장해 자동 계산한다.

---

## 단계 8 — 건물·호실 관리 화면 만들기

목표: 고정정보를 필요한 곳에서만 수정하고 과거 매물 이력은 안전하게 본다.

- [x] 건물 검색과 요약정보를 표시한다.
- [x] 건물 공통정보(주차·엘리베이터·공동현관 비밀번호 등)를 수정한다.
- [x] 선택한 건물의 호실 목록을 보여 준다.
- [x] 호실 구조·옵션·방향을 수정한다. 사진 파일·경로·링크는 관리하지 않는다.
- [x] 호실별 매물 이력을 날짜순으로 보여 준다.
- [x] 공동현관·방문 비밀번호는 `비밀번호 보기`를 눌렀을 때만 보인다.
- [x] 이번 매물에만 다른 옵션은 호실 기본 옵션이 아니라 `옵션 변경 메모`에 저장한다.

---

## 단계 9 — 엑셀 내보내기와 연락처 보호

목표: 조회한 매물 목록을 엑셀로 내보내되, 개인 연락처만 제외한다.

연락처 입력 원칙: 임대인·세입자 연락처는 건물 공통정보가 아니라, 실제 연락 대상이 달라질 수 있는 **매물 회차별 내부정보**로 저장한다. 새 매물 등록·재등록·현재 매물 수정 화면에서 입력·수정한다.

### 접수일 기준 목록·엑셀 내보내기 계획

목표: 저장된 매물 중 원하는 접수일 기간만 찾아, 그 결과를 엑셀 파일로 내려받는다.

- [x] `매물 현황 리스트`에 `접수일 시작`과 `접수일 종료` 필터를 추가한다.
- [x] 시작일만 입력하면 해당 날짜 이후 매물을, 종료일만 입력하면 해당 날짜 이전 매물을 보여 준다.
- [x] 두 날짜를 모두 입력하면 시작일과 종료일을 포함한 기간의 매물만 보여 준다.
- [x] 종료일이 시작일보다 빠르면 조회하지 않고 쉬운 안내문을 보여 준다.
- [x] 날짜 필터를 비우면 저장된 현재 매물 전체를 보여 준다.
- [x] 현재 화면의 날짜·검색·상태 등 필터 결과를 그대로 엑셀로 내보낼 수 있게 한다.
- [x] `현재 매물 전체 내보내기`와 `현재 조회 결과 내보내기`를 분명히 구분한다.
- [x] 내보내기 전 대상 범위와 건수를 안내한다.
- [x] 파일 이름에 내보낸 범위와 생성 시각을 넣는다.

### 구현 파일 역할

- [x] `ui/dashboard.py`: 접수일 시작·종료 입력, 현재 조회 결과 표시, 엑셀 다운로드
- [x] `storage/database.py`: 날짜·검색·상태 조건으로 현재 매물을 읽고, 선택한 현재 매물의 엑셀 항목을 읽는 기능
- [x] `services/export_service.py`: 선택한 현재 매물을 엑셀 파일로 만드는 규칙

- [x] `현재 매물 전체`와 `현재 조회 결과` 중 내보낼 범위를 고르게 한다.
- [x] 보증금·월세·관리비는 엑셀에서도 숫자로 저장한다.
- [x] 날짜는 엑셀에서 정렬할 수 있는 날짜 형식으로 저장한다.
- [x] 공동현관 비밀번호, 방문 비밀번호, 내부 메모는 포함한다.
- [x] 임대인·세입자의 개인 연락처와 상담관리의 고객 이름·연락처만 제외한다.
- [x] 다운로드 전 연락처만 제외된 내부 업무용 파일임을 안내한다.
- [x] 다운로드 파일 이름에 날짜·시간을 넣는다.

### 화면 확인

- [x] 엑셀 파일이 정상으로 열린다. (자동 확인)
- [x] 금액과 날짜가 숫자·날짜 형식으로 저장된다. (자동 확인)
- [x] 비밀번호와 내부 메모가 들어 있고, 연락처 관련 열이 없다. (자동 확인)
- [ ] 날짜 범위를 정해 조회한 결과와, 같은 조건으로 받은 엑셀의 매물 건수·목록이 일치한다. (브라우저 확인 필요)
- [x] 엑셀 파일 이름에 선택한 접수일 범위와 생성 시각이 들어 있다. (자동 확인)

---

## 단계 10 — 백업·복구·사무실 운영 준비

목표: 데이터가 실수나 PC 문제로 사라지지 않게 한다.

- [ ] 실제 데이터 파일은 운영 PC 한 곳에만 둔다.
- [ ] 사용자 PC는 브라우저로 운영 PC에 접속한다.
- [ ] 매일 업무 종료 뒤 SQLite 온라인 백업 방식으로 데이터 파일의 일관된 사본을 만든다.
- [ ] 일일 백업은 최근 10개만 순환 보관하고, 새 백업의 무결성 검사가 성공한 뒤에만 10개 초과분을 삭제한다.
- [ ] 일일 백업 10개는 원본 DB와 다른 물리 드라이브·외장 저장장치 또는 접근이 제한된 백업 위치에도 보관한다.
- [ ] 프로그램 업데이트·DB 구조 변경·대량 데이터 정정 전에는 프로그램과 DB의 별도 변경 전 백업을 만들고, 변경 안정화 전까지 보관한다.
- [ ] 백업 파일명에 날짜와 시간을 넣는다.
- [ ] 백업은 원본과 다른 드라이브 또는 제한된 접근 폴더에 보관한다.
- [ ] 백업마다 SQLite 무결성 검사와 성공·실패·파일 크기를 로그에 기록한다.
- [ ] 복구 전에는 백업본을 별도 위치에서 먼저 열어 확인한다.
- [ ] 프로그램 업데이트 시 코드는 바꾸되 운영 데이터 파일은 임의로 덮어쓰지 않는다.

### 운영 화면 확인

- [ ] 프로그램을 껐다 켜도 데이터가 남는다.
- [ ] 백업 파일이 만들어진다.
- [ ] 테스트 환경에서 백업본으로 정상 조회할 수 있다.
- [ ] 사무실 PC 2대에서 같은 매물 목록을 본다.

---

## 단계 11 — 계약관리 화면 만들기

목표: 가계약 진행부터 정식 계약·임대차 기간까지의 상태를 해당 매물 기록에 안전하게 누적한다.

- [x] 상단 메뉴에 `계약관리` 탭을 추가한다.
- [x] 계약은 건물·호실이 아니라 해당 시점의 **매물 회차**에 연결한다.
- [x] 계약 유형을 `일반 계약 / 단기계약 / 확인 필요` 중 선택한다.
- [x] 계약 진행 시작일, 정식 계약일, 임대차 시작일·종료일, 임대차 기간(개월), 계약 상태를 기록한다.
- [x] 계약금 전체와 가계약금 수령액, 남은 계약금 수령 예정일을 기록하고 계약금 미수령액을 자동 계산한다. 여러 입금 내역은 관리하지 않는다.
- [x] 계약 상태는 `계약 예정 / 계약 진행 / 계약 완료 / 해지 / 만료 / 확인 필요`로 관리한다.
- [x] 동일 호실의 계약을 여러 건 누적하고, 이전 계약을 덮어쓰지 않는다.
- [x] 예: 302호의 `5월 6개월 단기계약`, `11월 3개월 단기계약`을 각각 별도 계약 기록으로 남긴다.
- [x] 건물·호실 관리 화면에서는 계약 이력을 조회만 할 수 있게 한다.
- [x] 계약자 연락처와 계약금·잔금은 계약 기록의 내부정보로 입력·수정한다. 결제와 계약서 파일은 이번 범위에 넣지 않는다.

### 계약관리 화면 분리 계획

- [x] 계약관리 안을 `계약 등록`과 `계약 조회·수정` 화면으로 분리한다.
- [x] `계약 등록`에서는 매물 회차 검색·선택과 새 계약 입력·저장만 처리한다.
- [x] `계약 조회·수정`에서는 계약 목록 검색·필터, 선택한 계약의 진행 시작일·정식 계약일·임대차 기간·상태·연락처·계약금·잔금 수정을 처리한다.
- [x] 두 화면은 같은 계약 기록을 사용하며, 조회·수정 화면에서 새 계약을 중복으로 만들지 않는다.
- [x] 건물·호실 관리의 계약 이력은 계속 조회 전용으로 유지한다.
- [x] `계약 조회·수정`에서 오늘부터 30일 안에 종료되는 계약만 보는 `계약 만료 30일 전` 필터를 제공한다.
- [x] 만료 30일 전 필터에서는 이미 해지·만료된 계약과 종료일이 비어 있는 계약을 제외한다.
- [x] 계약 조회·수정에서 선택한 계약 기록을 확인 후 완전 삭제할 수 있다. 매물과 상담 기록은 유지한다.

### 화면 확인

- [x] 임시 매물 회차에 `5월 시작 / 6개월 / 단기계약`을 등록한다. (자동 확인)
- [x] 같은 호실의 다른 매물 회차에 `11월 시작 / 3개월 / 단기계약`을 등록한다. (자동 확인)
- [x] 두 계약이 모두 남고, 어느 매물 회차에 연결됐는지 확인한다. (자동 확인)
- [x] 계약을 해지 또는 만료로 바꿔도 기록이 삭제되지 않는지 확인한다. (자동 확인)

---

## 단계 12 — 상담관리 화면 만들기

목표: 매물별 상담·방문·다음 연락 기록을 간단히 누적해, 계약 전 진행 상황을 놓치지 않는다.

- [x] 상단 메뉴에 `상담관리` 탭을 추가한다.
- [x] 상담 등록에서 `매물 상담 / 일반 상담`을 선택한다. 매물 상담은 매물 회차에 연결하고, 일반 상담은 매물 없이 저장한 뒤 필요하면 나중에 매물 회차를 연결한다.
- [x] 상담일, 상담 종류(전화 / 문자 / 방문 / 기타), 상담 내용, 다음 연락일, 상담 상태를 기록한다.
- [x] 고객 이름 입력 없이 상담 기록을 저장하고, 연락처·내용 등은 필요할 때만 입력할 수 있게 한다.
- [x] 상담 상태는 `진행 중 / 보류 / 종료 / 확인 필요`로 관리한다.
- [x] 같은 매물에 여러 상담 기록을 날짜순으로 누적하고, 이전 기록을 덮어쓰지 않는다.
- [x] 다음 연락일이 오늘이거나 지났으면 상담관리 탭에서 확인할 수 있게 한다.
- [x] 고객 이름은 목록에, 연락처는 `연락처 보기`를 누른 상담 상세에만 표시한다. 기본 매물 목록·일반 엑셀에는 포함하지 않는다.
- [x] 일반 상담에는 희망 지역·방 형태·보증금·월세를 선택 입력할 수 있다.
- [x] 자동 문자 발송, 방문 예약 달력, 주민번호 등 민감 개인정보는 이번 범위에 넣지 않는다.

### 화면 확인

- [x] 임시 매물 회차에 전화 상담과 방문 상담을 각각 등록한다. (자동 확인)
- [x] 상담 두 건이 날짜순으로 남고, 다음 연락일이 지난 상담을 찾을 수 있다. (자동 확인)
- [x] 상담을 종료해도 기록이 삭제되지 않는지 확인한다. (자동 확인)
- [x] 상담 조회·수정에서 선택한 상담 기록을 확인 후 완전 삭제할 수 있다. 매물과 계약 기록은 유지한다.

---

## 단계 13 — 데이터 저장 코드 기능별 분리

목표: 실제 데이터 파일 `storage/real_estate.db`는 하나로 유지하고, 너무 커진 `storage/database.py`의 기능 코드를 업무별 파일로 나눠 유지보수를 쉽게 한다.

### 분리 원칙

- [x] SQLite 데이터 파일은 계속 하나만 사용한다. 건물·호실·매물·계약·상담 기록을 서로 다른 데이터 파일로 나누지 않는다.
- [x] `storage/database.py`에는 공통 연결, 표 생성·업데이트, 기존 데이터 파일에 새 칸을 안전하게 추가하는 기능만 남긴다.
- [x] 매물 등록·수정·조회는 `storage/listing_repository.py`, `storage/listing_write_repository.py`, `storage/listing_create_repository.py`로 분리한다.
- [x] 건물·호실 검색·상세·수정은 `storage/building_repository.py`로 분리한다.
- [x] 계약 등록·조회·수정은 `storage/contract_repository.py`로 분리한다.
- [x] 상담 등록·조회·수정은 `storage/consultation_repository.py`로 분리한다.
- [x] 엑셀용 매물 데이터 조회는 `storage/export_repository.py`로 분리한다.
- [x] 화면과 서비스 코드는 필요한 기능 파일만 불러오도록 바꾼다.

### 안전 규칙과 완료 기준

- [x] 정리 작업 중 기존 `real_estate.db` 파일을 새로 만들거나 덮어쓰지 않는다.
- [x] 표 이름, 기존 열 이름, 저장 규칙과 화면 동작을 바꾸지 않는다.
- [x] 한 기능 파일을 옮길 때마다 기존 저장·조회 자동 확인을 다시 실행한다.
- [ ] 분리 전후에 같은 데이터 파일의 건물·호실·매물·계약·상담 건수가 같음을 확인한다. (실제 운영 데이터는 안전을 위해 이번 코드 정리에서 열지 않음)
- [x] 기존 데이터가 그대로 보이고, 신규 등록·수정·계약·상담·엑셀 내보내기가 기존과 동일하게 동작한다.

---

## 3-1. 단계별 상세 구현계획과 테스트

### 공통 진행 원칙

각 단계는 아래 순서로 진행한다.

1. **작은 기능 하나를 만든다.** 예: 메뉴 하나, 저장 버튼 하나처럼 범위를 작게 잡는다.
2. **개발자가 확인한다.** 코드가 오류 없이 실행되고, 기본적인 자동 확인을 통과하는지 본다.
3. **브라우저에서 직접 확인한다.** 실제 사무실 사용자가 누르는 순서대로 눌러 본다.
4. **문제 기록 후 수정한다.** 오류 화면, 어떤 버튼을 눌렀는지, 기대한 결과를 `결정·수정 기록`에 남긴다.
5. **다시 확인한다.** 수정한 부분뿐 아니라 관련된 기존 기능도 한 번 더 확인한다.

테스트 결과 표기:

- **PASS**: 기대한 대로 동작함
- **FAIL**: 다르게 동작하거나 업무를 할 수 없음
- **BLOCKED**: 앞선 오류나 PC 환경 문제로 시험할 수 없음

### 단계 1 상세 — 실행 가능한 빈 화면

#### 구현 순서

1. 프로그램 시작 파일(`app.py`)을 만든다.
2. 화면 이름, 사무실 내부용 안내문, 기본 넓이를 설정한다.
3. 메뉴 상태를 한 곳에서 관리해 메뉴를 눌러도 화면 선택이 흔들리지 않게 한다.
4. 각 메뉴 화면은 우선 제목·설명·빈 상태만 보여 주는 함수로 나눈다. 이렇게 하면 다음 단계에서 한 화면을 고쳐도 다른 화면에 영향을 덜 준다.
5. 데이터가 없을 때 등록 화면으로 이동하는 버튼을 연결한다.

#### 테스트 및 합격 기준

| ID | 직접 할 일 | 기대 결과 | 관련 기준 |
|---|---|---|---|
| S1-01 | 프로그램을 실행한다. | 오류 없이 브라우저가 열리고 `매물 현황 리스트`가 먼저 보인다. | TC-036 |
| S1-02 | 세 메뉴를 차례로 누른다. | 선택한 메뉴의 제목과 내용이 각각 보인다. | 화면 명세 2장 |
| S1-03 | 데이터가 없는 상태에서 첫 화면을 본다. | 빈 표가 아니라 첫 매물 등록 안내와 버튼이 보인다. | TC-043 |
| S1-04 | 등록 안내 버튼을 누른다. | `매물 등록·수정` 화면으로 이동한다. | 화면 명세 7.4 |

**단계 완료 조건:** S1-01~04가 모두 PASS이고, 화면을 처음 보는 사람이 메뉴와 등록 버튼을 찾을 수 있다.

### 단계 2 상세 — 데이터 저장 뼈대

#### 구현 순서

1. 데이터 파일의 위치를 설정 파일 또는 한 곳의 상수로 정한다. 코드 여러 곳에 경로를 적지 않는다.
2. 건물·호실·매물 회차용 표를 만든다. 각 기록에는 내부 번호, 만든 시각, 수정 시각을 둔다.
3. 건물과 호실, 호실과 매물 회차의 연결 규칙을 만든다.
4. 데이터 파일이 없거나 경로가 틀렸을 때의 안내를 만든다.
5. 저장, 한 건 조회, 목록 조회 기능을 화면과 분리된 코드로 만든다. 화면 디자인을 고쳐도 저장 규칙이 깨지지 않게 하기 위함이다.
6. 저장 중 오류가 나면 중간 데이터가 남지 않도록 “모두 저장 또는 모두 취소” 처리한다.

#### 테스트 및 합격 기준

| ID | 직접 할 일 | 기대 결과 | 관련 기준 |
|---|---|---|---|
| S2-01 | 테스트용 건물·호실·매물을 저장하고 프로그램을 종료한 뒤 다시 연다. | 같은 정보가 그대로 보인다. | TC-051 |
| S2-02 | 한 건물에 201호와 302호를 저장한다. | 두 호실이 같은 건물 아래에 보인다. | 데이터 구조 |
| S2-03 | 302호에 서로 다른 매물 2건을 저장한다. | 매물 2건이 302호 이력으로 연결된다. | TC-014 |
| S2-04 | 의도적으로 저장 중 오류가 나게 만든다. | 건물만 또는 호실만 남는 일이 없다. | TC-056 |
| S2-05 | 존재하지 않는 데이터 파일 경로로 실행한다. | 조용히 빈 데이터를 만들지 않고 원인을 안내한다. | TC-055 |

**단계 완료 조건:** S2-01~05가 PASS이며, 저장 구조를 모르는 사람도 “과거 매물은 남는다”는 결과를 화면에서 확인할 수 있다.

### 단계 3 상세 — 신규 등록

#### 구현 순서

1. 입력 화면을 `건물`, `호실`, `이번 매물` 세 구역으로 나눈다.
2. 처음에는 필수 입력칸만 보이고, 관리비·옵션·현장 상태 같은 자세한 칸은 접어서 보여 준다.
3. 가격 입력칸 옆에 항상 `(만원)`을 표시한다.
4. 저장 버튼을 누르기 전에 빠진 값과 잘못된 값을 한 번에 검사한다.
5. 오류는 해당 입력칸 바로 아래에 쉬운 말로 보여 준다.
6. 저장 직전 요약 내용을 보여 준 뒤, 저장 성공 시 등록한 매물 상세 또는 현황 화면으로 이동한다.

#### 테스트 및 합격 기준

| ID | 직접 할 일 | 기대 결과 | 관련 기준 |
|---|---|---|---|
| S3-01 | 대성빌 / 북수리 1026 / 302 / 500 / 45를 입력해 저장한다. | 건물 1건, 호실 1건, 매물 1건이 한 번에 생성된다. | TC-001 |
| S3-02 | 건물명 또는 지번을 비우고 저장한다. | 저장되지 않고 비어 있는 항목의 이유가 보인다. 입력 내용은 유지된다. | TC-002 |
| S3-03 | 호수를 비우고 저장한다. | 저장되지 않고 호수 입력 안내가 보인다. | TC-003 |
| S3-04 | 보증금에 `오백`, 월세에 `45만원`을 넣는다. | 숫자만 입력하라는 안내가 보이고 저장되지 않는다. | TC-004 |
| S3-05 | 입주 유형을 날짜 지정으로 고른 뒤 날짜를 비운다. | 날짜 입력 안내가 보이고 저장되지 않는다. | TC-005 |
| S3-06 | 선택 입력칸을 비우고 필수값만 넣어 저장한다. | 정상 저장되며 정보 상태는 기본등록이다. | TC-006 |

**단계 완료 조건:** S3-01~06이 PASS이고, 잘못 입력해도 이미 적은 내용이 사라지지 않는다.

### 단계 4 상세 — 검색과 중복 방지

#### 구현 순서

1. 건물명과 지번을 같은 검색칸에서 찾게 한다.
2. 입력 중 또는 검색 버튼 클릭 시 기존 건물 후보를 보여 준다.
3. 호수는 저장 전에 `호`를 제외한 비교용 값으로 바꿔 중복을 검사한다. 화면에는 사람이 읽기 좋은 표기를 유지한다.
4. 기존 건물을 골랐을 때는 건물 공통정보를 잠그거나 접어서 다시 저장하지 않게 한다.
5. 중복을 발견했을 때는 단순 오류가 아니라 “기존 건물 선택” 또는 “현재 매물 수정” 같은 다음 행동을 안내한다.

#### 테스트 및 합격 기준

| ID | 직접 할 일 | 기대 결과 | 관련 기준 |
|---|---|---|---|
| S4-01 | 이미 있는 대성빌 또는 북수리 1026을 검색한다. | 기존 대성빌이 새 등록보다 먼저 보인다. | TC-007 |
| S4-02 | 대성빌에 302호를 새로 등록하려 한다. | 기존 302호를 안내하고 새 호실 생성을 막는다. | TC-008 |
| S4-03 | 기존 `302`가 있는 상태에서 `302호`를 등록한다. | 같은 호실로 판단해 중복 등록을 막는다. | TC-009 |
| S4-04 | 건물명은 대성빌, 지번은 다른 값으로 등록한다. | 자동으로 기존 건물에 합쳐지지 않는다. | TC-010 |
| S4-05 | 대성빌에 없는 201호를 등록한다. | 건물 정보 재입력 없이 201호와 첫 매물을 저장한다. | TC-011 |

**단계 완료 조건:** S4-01~05가 PASS이며, 중복이 생길 수 있는 순간마다 사용자에게 선택 이유가 보인다.

### 단계 5 상세 — 기존 호실 최신 정보 수정

#### 구현 순서

1. 기존 건물과 호실을 고르는 순서를 만든다.
2. 건물·호실의 고정정보와 현재 매물 조건을 자동으로 불러온다.
3. 사용자는 바뀐 값만 수정하고 `최신 정보 저장`으로 같은 매물 기록을 갱신한다.
4. 저장 뒤 계약·상담 기록이 계속 같은 매물 기록에 연결됐는지 자동 확인한다.

#### 테스트 및 합격 기준

| ID | 직접 할 일 | 기대 결과 | 관련 기준 |
|---|---|---|---|
| S5-01 | 대성빌 302호를 선택해 최신 정보 수정을 시작한다. | 구조·방향·옵션·방문 방법과 현재 조건이 보인다. | TC-012 |
| S5-02 | 현재 500/45를 확인한다. | 입력칸에 기본값으로 보이며 필요한 값만 수정할 수 있다. | TC-013 |
| S5-03 | 500/47, 공실, 즉시입주로 최신 정보를 저장한다. | 새 회차 없이 현재 매물의 조건이 바뀌고 계약·상담 연결이 유지된다. | TC-014 |
| S5-04 | 가격을 모르는 상태로 저장하려 한다. | 0으로 저장하지 않고 가격 확인 필요로 남긴다. | TC-015 |
| S5-05 | 종료되지 않은 매물이 있을 때 기존 호실을 선택한다. | 최신 정보 입력칸이 바로 열리고, 저장 시 같은 매물 기록이 갱신된다. | TC-016 |
| S5-06 | 기존 비밀번호만 보고 실제 확인 없이 저장한다. | 비밀번호의 마지막 확인일이 오늘로 바뀌지 않는다. | TC-017 |

**단계 완료 조건:** S5-01~06이 PASS이며, 과거 매물의 가격·상태·날짜가 새 등록 때문에 바뀌지 않는다.

### 단계 6 상세 — 현재 매물 수정과 종료

#### 구현 순서

1. 현재 매물의 상세 화면에 `현재 매물 수정`을 둔다.
2. 수정 화면은 현재 회차의 값만 바꾸도록 연결한다.
3. 저장 전 변경한 항목을 요약해 보여 준다.
4. 종료 화면에는 종료 사유와 종료일을 필수 확인 항목으로 둔다.
5. 매물 완전 삭제는 명시적 확인 후에만 실행하고, 연결된 계약·상담 기록도 함께 삭제한다. 잘못 만든 호실도 영향 건수를 보여 준 뒤 명시적 확인 후 완전 삭제할 수 있으며, 건물은 유지한다.

#### 테스트 및 합격 기준

| ID | 직접 할 일 | 기대 결과 | 관련 기준 |
|---|---|---|---|
| S6-01 | 월세를 47에서 45로 수정한다. | 새 회차 없이 현재 회차의 월세와 수정일만 바뀐다. | TC-018 |
| S6-02 | 날짜 지정 매물을 즉시입주로 바꾼다. | 목록에는 즉시입주가 보이고 과거 날짜가 현재 조건으로 남지 않는다. | TC-019 |
| S6-03 | 사진 보유 여부를 없음에서 있음으로 바꾼다. | 사진 촬영 필요 목록에서 사라진다. | TC-020 |
| S6-04 | 상태·가격·입주일·메모를 여러 개 바꾼다. | 저장 버튼을 누르기 전에는 반영되지 않고, 누르면 한 번에 반영된다. | TC-021 |
| S6-05 | 계약 완료와 정보 오류 사유로 각각 종료한다. | 현재 목록에서는 제외되지만 이력에서는 두 기록을 볼 수 있다. | TC-025, TC-027 |
| S6-06 | 계약 여부를 모르는 매물을 처리한다. | 자동 계약 완료가 되지 않고 확인 필요 또는 보류로 둘 수 있다. | TC-026 |

**단계 완료 조건:** S6-01~06이 PASS이고, 화면의 버튼만 봐도 수정과 종료를 구분할 수 있다.

### 단계 7 상세 — 매물 현황 리스트

#### 구현 순서

1. 현재 활성 매물만 기본 목록에 가져온다.
2. 상단 요약 숫자와 실제 목록의 개수가 같은 기준으로 계산되게 한다.
3. 검색어와 각 필터를 조합해 하나의 목록 결과를 만든다.
4. 확인 업무를 별도 할 일 표에 저장하지 않고, 매물의 상태·날짜에서 계산한다.
5. 작은 화면에서도 상태·건물·호수·가격·해야 할 일이 우선 보이게 표 열 순서를 조정한다.
6. 행 선택 시 다른 화면으로 나가지 않고 아래에서 상세를 펼친다.

#### 테스트 및 합격 기준

| ID | 직접 할 일 | 기대 결과 | 관련 기준 |
|---|---|---|---|
| S7-01 | 오늘 접수된 매물만 선택한다. | 요약 숫자와 표의 건수가 일치한다. | TC-029 |
| S7-02 | 퇴실 예정, 재확인 필요, 사진 촬영 필요를 각각 누른다. | 해당 조건의 활성 매물만 보인다. | TC-030~032 |
| S7-03 | 대성빌, 북수리 1026, 302를 각각 검색한다. | 모두 대성빌 302호를 찾는다. | TC-033 |
| S7-04 | 룸 형태·상태·사진 촬영 필터를 함께 적용한다. | 세 조건을 모두 만족하는 매물만 보인다. | TC-034 |
| S7-05 | 필터 초기화를 누른다. | 필터가 기본값으로 돌아가고 전체 활성 목록이 보인다. | TC-035 |
| S7-06 | 매물 행의 상세를 누른다. | 표를 떠나지 않고 아래에 상세가 펼쳐진다. | TC-038 |
| S7-07 | 기본 목록과 상세를 확인한다. | 기본 목록에는 비밀번호·내부 메모가 없다. | TC-044 |
| S7-08 | 접수일 시작을 8월 1일, 종료를 8월 31일로 선택한다. | 두 날짜를 포함한 8월 접수 매물만 보인다. | 날짜 필터 추가 기준 |
| S7-09 | 시작일만 또는 종료일만 선택한다. | 각각 이후 또는 이전 접수 매물만 보인다. | 날짜 필터 추가 기준 |
| S7-10 | 종료일을 시작일보다 빠르게 선택한다. | 조회하지 않고 날짜 순서를 바꾸라는 안내가 보인다. | 날짜 필터 추가 기준 |

**단계 완료 조건:** S7-01~07이 PASS이고, 사무실 PC 화면에서 가로로 크게 움직이지 않고 핵심 정보를 읽을 수 있다.

### 단계 8 상세 — 건물·호실 관리

#### 구현 순서

1. 건물 검색 → 건물 요약 → 호실 목록 → 호실 상세 순서로 화면을 구성한다.
2. 건물 공통정보와 호실 고정정보의 저장 버튼을 분리한다.
3. 비밀번호는 기본적으로 별표 처리하고, `비밀번호 보기`를 누른 경우에만 잠깐 표시한다.
4. 호실 상세에는 현재 매물과 과거 매물 이력을 분리해 보여 준다.
5. 이번 매물만 달라지는 옵션은 매물 회차에만 저장하도록 화면 문구를 명확히 한다.

#### 테스트 및 합격 기준

| ID | 직접 할 일 | 기대 결과 | 관련 기준 |
|---|---|---|---|
| S8-01 | 대성빌의 공동현관 비밀번호를 수정하거나 삭제한다. | 같은 건물 호실 상세에서 바뀐 값을 확인할 수 있고, 삭제 후에는 `등록되지 않음`으로 표시된다. | TC-022 |
| S8-02 | 302호 방향을 수정한 뒤 최신 정보 수정을 시작한다. | 최신 정보 화면에 수정된 방향이 불러와진다. | TC-023 |
| S8-03 | 이번 매물에만 다른 옵션을 입력한다. | 호실 기본 옵션은 유지되고 이번 회차 메모에만 저장된다. | TC-024 |
| S8-04 | 비밀번호 보기 전후를 확인한다. | 누르기 전에는 보이지 않고, 누른 뒤 상세에서만 보인다. | TC-045 |
| S8-05 | 잘못 만든 호실을 삭제하려 한다. | 삭제될 매물·계약·상담 건수를 확인한 뒤 호실만 완전 삭제되고 건물은 남는다. | TC-028 |

**단계 완료 조건:** S8-01~05가 PASS이고, 고정정보 수정이 과거 매물 조건을 바꾸지 않는다.

### 단계 9 상세 — 엑셀 내보내기와 연락처 보호

#### 구현 순서

1. 엑셀에 보낼 열 목록을 코드에서 명시적으로 정한다. 현재 매물의 비밀번호·내부 메모를 포함하되, 임대인·세입자 개인 연락처와 상담관리 고객 이름·연락처는 제외한다.
2. 전체 현재 매물과 현재 검색 결과 중 하나를 선택하게 한다.
3. 금액은 숫자, 날짜는 날짜 형태로 내보낸다.
4. 파일 생성 전에 내보낼 건수와 제외되는 내부정보를 안내한다.
5. 만들어진 파일 이름에는 생성 날짜와 시간을 붙인다.

#### 테스트 및 합격 기준

| ID | 직접 할 일 | 기대 결과 | 관련 기준 |
|---|---|---|---|
| S9-01 | 전체 현재 매물과 현재 조회 결과를 각각 내보낸다. | 선택한 범위가 파일에 정확히 들어간다. | TC-048 |
| S9-02 | 파일을 열어 금액·날짜 열을 정렬한다. | 숫자·날짜로 정렬되며 글자처럼 섞이지 않는다. | TC-049 |
| S9-03 | 파일의 모든 열을 확인한다. | 공동현관·방문 비밀번호, 내부 메모, 열쇠 위치는 포함되고, 임대인·세입자 개인 연락처와 상담관리 고객 이름·연락처는 없다. | TC-046 |
| S9-04 | 메뉴와 화면을 확인한다. | 기존 엑셀 가져오기 기능은 없다. | TC-050 |
| S9-05 | 접수일 8월 1일~8월 31일로 조회한 뒤 `현재 조회 결과`를 내보낸다. | 화면의 건수·매물 목록과 엑셀의 건수·매물 목록이 같다. | 날짜 내보내기 추가 기준 |
| S9-06 | 날짜 필터를 비운 뒤 `현재 매물 전체`를 내보낸다. | 기간 제한 없이 전체 현재 매물이 들어간다. | 날짜 내보내기 추가 기준 |
| S9-07 | 날짜 범위를 고른 엑셀 파일 이름을 확인한다. | 시작일·종료일·생성 시각을 확인할 수 있다. | 날짜 내보내기 추가 기준 |

**단계 완료 조건:** S9-01~04가 PASS이고, 엑셀 파일에 개인 연락처가 포함되지 않는다.

### 단계 10 상세 — 백업·복구·운영

#### 구현 순서

1. 운영 PC의 프로그램 폴더, 데이터 폴더, 백업 폴더를 분리한다.
2. 백업은 원본을 복사하는 방식으로 만들며, 원본을 옮기거나 지우지 않는다.
3. 일일·주간·월간 백업 이름과 보관 개수 규칙을 구현한다.
4. 백업 성공·실패와 파일 위치를 기록한다.
5. 복구는 항상 별도 검증 위치에서 먼저 열어 보고, 운영 데이터 교체는 프로그램을 끈 뒤에만 한다.
6. 운영 PC와 다른 사용자 PC의 접속 방법, 장애 시 확인 순서를 짧은 안내문으로 만든다.

#### 테스트 및 합격 기준

| ID | 직접 할 일 | 기대 결과 | 관련 기준 |
|---|---|---|---|
| S10-01 | 프로그램 종료 후 다시 실행한다. | 저장한 데이터가 그대로 보인다. | TC-051 |
| S10-02 | 운영 PC를 재시작한 뒤 정해진 방법으로 실행한다. | 기존 데이터로 정상 실행된다. | TC-052 |
| S10-03 | 일일 백업을 실행한다. | 원본과 다른 위치에 날짜·시간이 든 백업이 만들어진다. | TC-053 |
| S10-04 | 백업본을 별도 테스트 폴더에서 연다. | 건물·호실·최근 매물 이력을 정상 조회한다. | TC-054 |
| S10-05 | 사용자 PC 두 대에서 운영 PC 주소로 접속한다. | 두 화면에서 같은 목록을 본다. | TC-057 |
| S10-06 | 한 PC에서 새 매물을 저장하고 다른 PC에서 새로 조회한다. | 다른 PC에도 새 매물이 보인다. | TC-058 |
| S10-07 | 운영 PC의 프로그램을 종료한 뒤 사용자 PC에서 접속한다. | 데이터 손상 없이 운영 PC 실행 필요 안내로 원인을 알 수 있다. | TC-060 |

**단계 완료 조건:** S10-01~07이 PASS이고, 백업본으로 실제 조회까지 성공했다.

---

## 추가 운영 계획 — 개발 완료 후 메인 PC 이전 및 2인 전용 접속

목표: 개발 PC에서 기능·저장·백업 확인을 모두 끝낸 뒤에만 프로젝트 전체를 사무실 메인 PC로 옮기고, 두 명만 안전하게 접속한다. 개발 중에는 이전하거나 운영 데이터를 분산하지 않는다.

### 운영 방식 결정

- [x] 초기 운영은 **메인 PC + 현재 SQLite 데이터 파일 + Tailscale** 방식으로 한다.
- [ ] Streamlit Community Cloud에는 운영 데이터 파일을 올리지 않는다. 로컬 파일 저장은 영구 보관이 보장되지 않고, 연락처·비밀번호가 포함된 데이터 파일을 GitHub에 보관하면 안 된다.
- [ ] 메인 PC는 사무실의 항상 켜진 PC 또는 전용 미니 PC로 정한다.
- [ ] 두 사용자만 각자의 Tailscale 계정으로 메인 PC에 접속하도록 허용한다.
- [ ] 외부에서도 접속할 때는 일반 인터넷 주소를 공개하지 않고, Tailscale 개인 네트워크 안에서만 접속한다.
- [ ] 매물 데이터 파일은 **메인 PC의 `storage\real_estate.db` 한 곳에서만** 사용한다. 사용자 PC에는 데이터 파일을 복사하거나 직접 열지 않는다.
- [ ] 메인 PC에서 프로그램은 본인 PC에서만 열리도록 실행하고, Tailscale Serve를 통해서만 두 사용자 PC에 연결한다. 일반 인터넷에 공개하는 Tailscale Funnel은 사용하지 않는다.

### 개발 완료 후 이전 순서

1. 개발 PC에서 신규 등록·최신 정보 수정·계약·상담·엑셀·백업 확인을 끝낸다.
2. 프로그램을 완전히 종료한 뒤, 프로젝트 폴더 전체를 메인 PC의 고정 위치(예: `C:\realestate`)로 복사한다.
3. 반드시 `storage\real_estate.db`와 백업 폴더를 함께 복사한다. 데이터 파일만 따로 복사하거나 GitHub에 올리지 않는다.
4. 메인 PC에 Python 3.12 환경과 필요한 패키지를 설치하고, `streamlit run app.py`로 먼저 메인 PC에서 실행 확인한다.
5. 메인 PC와 두 사용자 기기에 Tailscale을 설치하고, 두 사용자 계정만 메인 PC 접속 권한을 준다.
6. 메인 PC에서 Tailscale Serve를 설정해, 메인 PC에서만 실행 중인 프로그램을 Tailscale 내부 주소로 연결한다. 인터넷에 공개하는 기능(Funnel)은 켜지 않는다.
7. 두 사용자 PC에서 안내받은 Tailscale 내부 주소를 브라우저로 열어 같은 매물 목록을 보는지, 한쪽에서 저장한 내용이 다른 쪽에 보이는지 확인한다.
8. 이전 완료 후 개발 PC의 데이터 파일은 즉시 삭제하지 않고, 읽기 전용 백업본으로 보관한다.

### 이전 완료 기준

- [ ] 메인 PC에서 기존 건물·호실·매물·계약·상담 기록이 모두 보인다.
- [ ] 두 사용자만 접속할 수 있고, 제3자가 일반 인터넷 주소로 접속할 수 없다.
- [ ] 한 사용자의 저장·수정 결과를 다른 사용자가 새로 조회해 확인할 수 있다.
- [ ] 메인 PC 재시작 후에도 앱과 데이터가 정상 실행된다.
- [ ] 이전 전 데이터 파일과 이전 후 데이터 파일의 건물·호실·매물·계약·상담 건수가 같다.
- [ ] 백업 파일을 별도 위치에서 열어 조회하는 복구 확인을 마쳤다.
- [ ] 사용자 PC에 `real_estate.db` 파일이 없고, 모든 저장은 메인 PC 데이터 파일 한 곳에 반영된다.

### 향후 전환 검토 기준

- 메인 PC를 계속 켜둘 수 없거나, 사무실 밖에서도 PC 상태와 무관하게 접속해야 하면 Supabase 같은 외부 데이터베이스 전환을 별도 단계로 검토한다.
- 외부 데이터베이스 전환 시에는 두 사용자 로그인, 접근 권한 제한, 비밀번호·연락처 보호 방식, 기존 SQLite 데이터 이전과 복구 시험을 먼저 설계한다.

---

## 3-2. 단계 완료 기록표

| 단계 | 완료일 | 개발 확인 | 브라우저 확인 | 핵심 테스트 결과 | 남은 문제 |
|---|---|---|---|---|---|
| 1. 빈 화면 | 2026-08-01 | PASS | 자동 테스트 PASS · 실제 브라우저 확인 필요 | S1-01~04 자동 확인 PASS | 브라우저에서 화면 배치 직접 확인 |
| 2. 데이터 저장 | 2026-08-01 | PASS | 자동 테스트 PASS · 실제 브라우저 확인 필요 | 저장·재조회·중간 저장 방지 PASS | 데이터 파일 위치·0건 표시 직접 확인 |
| 3. 신규 등록 | 2026-08-01 | PASS | 자동 테스트 PASS · 실제 브라우저 확인 필요 | 입력 검사·저장·상세값 재조회 PASS | 실제 신규 등록 1건 화면 확인 |
| 4. 검색·중복 방지 |  |  |  |  |  |
| 5. 재등록 |  |  |  |  |  |
| 6. 수정·종료 |  |  |  |  |  |
| 7. 오늘의 현황 |  |  |  |  |  |
| 8. 건물·호실 관리 |  |  |  |  |  |
| 9. 엑셀·보호 |  |  |  |  |  |
| 10. 백업·운영 |  |  |  |  |  |
| 11. 계약관리 |  |  |  |  |  |
| 12. 상담관리 |  |  |  |  |  |

---

## 4. 화면을 보면서 결정할 항목

아래는 실제로 사용해 본 뒤 결정한다. 임의로 복잡하게 만들지 않는다.

- [ ] 룸 형태에 `주인세대`, `상가주택`을 기본 선택지로 넣을지
- [ ] 주차를 단순 상태로 둘지, 주차 대수까지 적을지
- [ ] 관리비 포함 항목을 체크 방식으로 나눌 시점
- [ ] 단기계약 가격을 별도 숫자로 관리할지
- [ ] 광고 상태를 기본 목록에 항상 보여 줄지
- [ ] `확인완료`가 되기 위한 최소 정보 기준
- [ ] 사진 보유 여부를 `있음 / 없음 / 확인 필요` 중 어떤 기준으로 판단할지 실제 사용 후 보완한다.
- [ ] 표에서 가장 자주 보는 열의 순서와 폭

---

## 5. 배포 전 최소 확인 목록

- [ ] 신규 건물·호실·첫 매물 등록이 된다.
- [ ] 같은 호실 최신 정보 수정 시 새 매물 회차가 생기지 않는다.
- [ ] 최신 정보 수정 뒤 계약·상담 기록이 유지된다.
- [ ] 재확인·사진 촬영 필요 매물을 찾을 수 있다.
- [ ] 비밀번호는 기본 목록에 나오지 않고, 엑셀에는 포함된다. 개인 연락처는 엑셀에 나오지 않는다.
- [ ] 프로그램을 다시 실행해도 데이터가 남는다.
- [ ] 백업을 만들고 복구 확인을 해 봤다.
- [ ] 사용자 PC 2대에서 같은 데이터가 보인다.
- [ ] 치명적인 문제(데이터 유실, 저장 불가, 과거 기록 덮어쓰기)가 없다.
- [ ] 실제 사용자가 설명 없이 `대성빌 302호` 신규 등록과 최신 정보 수정을 완료한다.

---

## 6. 결정·수정 기록

| 날짜 | 화면/기능 | 직접 확인한 내용 | 결정 또는 수정 | 다음 확인 |
|---|---|---|---|---|
| 2026-08-01 | 1단계 화면 | 상단 메뉴와 첫 등록 안내를 직접 확인함 | 일부 화면 요소가 잘리는 부분은 다음 UI 개선 단계에서 조정 | 2단계 데이터 저장 뼈대 구현 |
| 2026-08-03 | 사진 관리 범위 | 사진 파일과 보관 위치는 이 도구에서 관리하지 않기로 결정 | 등록 화면의 사진 경로 입력과 외부 링크 검사 제거 | 7단계에서 사진 보유 여부 선택·필터·촬영 필요 표시 구현 |
| 2026-08-03 | 새 매물 등록 | 이전 검색·입력·선택 상태가 남아 새 등록을 시작하기 불편함 | 상단 `새 매물 등록` 클릭 시 모든 등록 관련 상태를 초기화 | 실제 브라우저에서 반복 등록 흐름 확인 |
| 2026-08-06 | 사진 관리 | 사진 상태와 사진 보유 여부를 따로 입력하면 서로 엇갈릴 수 있음 | 사진 상태 입력·표시·필터를 없애고 사진 보유 여부 하나로 사진 촬영·확인 필요 업무를 자동 표시 | 실제 브라우저에서 신규 등록·최신 정보 수정·현황 빠른 수정·필터 확인 |
| 2026-08-06 | 매물 등록·수정 | 연락처를 숫자로 입력할 때 읽기 어려움 | 임대인·세입자 연락처에 숫자를 입력하거나 붙여넣으면 국내 전화번호 형식의 하이픈을 자동 표시 | 실제 브라우저에서 세 입력 화면 확인 |
| 2026-08-06 | 상담관리 | 고객 연락처를 숫자로 입력할 때 읽기 어려움 | 상담 등록·수정에서 저장 시 고객 연락처를 국내 전화번호 형식의 하이픈으로 자동 저장 | 실제 브라우저에서 일반·매물 상담 등록과 수정 화면 확인 |
| 2026-08-06 | 상담관리 | 매물 상담에서는 고객 희망 조건을 기록할 수 없고, 상담 상태 `확인 필요`가 목록 업무로 드러나지 않음 | 일반·매물 상담 모두에 희망 조건 선택 입력을 제공하고, 목록 `해야 할 일`에 상담 확인·다음 연락 필요를 함께 표시 | 실제 브라우저에서 매물 상담 희망 조건 저장과 목록 표시 확인 |
| 2026-08-06 | 상담관리 | 일반 상담 저장 완료 문구가 즉시 다시 실행되어 실무자가 등록 여부를 확인하기 어려움 | 일반·매물 상담 모두 등록 뒤 다음 화면에도 완료 안내를 표시 | 실제 브라우저에서 두 상담 유형의 등록 완료 안내 확인 |
| 2026-08-06 | 건물·호실 관리 | 호실 번호 오입력 시 기존 호실을 삭제하고 다시 등록하면 연결 이력이 사라질 수 있음 | 호실 번호 정정 기능으로 같은 호실 기록의 번호만 바꾸고 매물·계약·상담 이력은 유지 | 실제 브라우저에서 304호 → 302호 정정과 중복 번호 차단 확인 |
| 2026-08-06 | 매물 등록 | 현장에서 건물명을 모르는 매물은 등록이 막힘 | 건물명은 선택 입력으로 바꾸고, 비우면 `건물명 미입력`으로 저장하되 지번·호수 중복 방지는 유지 | 실제 브라우저에서 건물명 없이 신규 등록 확인 |
| 2026-08-06 | 매물 등록·수정 | 전세처럼 월세가 없는 매물은 보증금·월세 필수 검사 때문에 등록할 수 없음 | 보증금·월세를 각각 선택 입력으로 바꾸고, 비운 금액은 목록에서 `-`로 표시 | 실제 브라우저에서 보증금만 입력한 전세 매물 저장 확인 |
| 2026-08-06 | 매물 등록 | 실제 호실 구조를 더 정확히 분류할 룸 형태가 필요함 | 공통 룸 형태 목록에 `투베이`, `쓰리베이`를 추가 | 신규 등록·수정·현황 필터에서 선택값 확인 |
| 2026-08-06 | 매물 등록 | 호실 옵션에 전자레인지 보유 여부를 빠르게 기록할 필요가 있음 | 호실 옵션 공통 체크박스에 `전자레인지`를 추가 | 신규 등록·최신 정보 수정에서 선택·저장 확인 |
| 2026-08-06 | 매물 현황 리스트 | 기본 현재 매물 표에서 비어 있는 종료일·종료 사유 열이 업무 판단을 방해함 | 현재 매물만 조회에서는 두 열을 숨기고, 종료된 매물 또는 전체 조회에서만 표시 | 실제 브라우저에서 세 조회 범위 확인 |
| 2026-08-06 | 운영 데이터 정정 | 접수일이 2026-07-28로 잘못 저장된 현재 매물 7건을 확인 | 대상 7건의 접수일을 2026-08-06으로 변경하고, 기존 날짜 잔여 0건을 재조회로 확인 | 현황 리스트에서 접수일 표시 확인 |
| 2026-08-06 | 계약 등록 | 선택 입력칸이 비어 있어도 기록할 수 있는데 별도 확인 체크 때문에 저장 버튼이 비활성화됨 | 매물 회차를 선택하면 `새 계약 등록` 버튼을 바로 사용하게 하고, 필수 계약값 검사는 그대로 유지 | 실제 브라우저에서 빈 선택값 계약 등록 확인 |
| 2026-08-06 | 계약관리 | 가계약금 수령부터 정식 계약·입주까지의 날짜를 계약 시작일 하나로 기록하면 의미가 섞임 | 계약 진행 시작일·정식 계약일·임대차 시작일·종료일을 분리하고, 계약 진행 기록을 나중에 정식 계약으로 이어서 수정 | 실제 브라우저에서 가계약 → 정식 계약 흐름 확인 |
| 2026-08-06 | 계약 데이터 구조 | 계약 진행·정식 계약·임대차 기간을 분리하려면 기존 필수 계약 시작일을 선택값으로 바꿔야 함 | 변경 전 `backups\real_estate_before_contract_workflow_20260806.db` 사본을 만들고, 기존 계약 수 보존을 확인한 뒤 호환 마이그레이션 적용 | 실제 브라우저에서 계약 등록·수정 확인 |
| 2026-08-06 | 계약관리 | 계약금 전체 중 가계약금만 일부 수령한 상태를 계약금·잔금만으로는 구분할 수 없음 | 계약금 전체·가계약금 수령액·추가 수령 예정일을 기록하고 계약금 미수령액을 자동 표시 | 실제 브라우저에서 100/50/900 입력 확인 |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
