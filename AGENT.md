# 부동산 매물관리 도구 전체 빌드 규칙

> 이 문서는 이 프로젝트를 만들거나 고칠 때 모든 작업자가 반드시 지켜야 할 기준이다.
>
> 가장 중요한 목표: 사무실 직원이 엑셀보다 빠르고 안전하게 매물을 등록·확인하도록 돕는다.

## 1. 프로젝트의 성격과 우선순위

- 이 프로그램은 고객용 웹사이트가 아니라 **사무실 내부용 매물관리 도구**다.
- 주요 사용자는 개발자가 아닌 사무실 직원 1~2명이다. 화면 문구와 오류 안내는 쉬운 한국어로 쓴다.
- 화려한 디자인보다 `검색`, `표`, `빠른 입력`, `안전한 저장`을 우선한다.
- 화면의 모든 요소는 “지금 해야 할 업무를 빨리 결정하게 돕는가?”를 기준으로 추가한다.
- 1차 버전에서 정하지 않은 기능은 임의로 넓히지 않는다. 실제 사용 필요성이 반복 확인된 뒤 추가한다.

우선순위는 아래 순서다.

1. 데이터가 사라지거나 과거 기록이 바뀌지 않게 한다.
2. 신규 등록과 재등록을 빠르고 헷갈리지 않게 한다.
3. 오늘 확인할 매물을 쉽게 찾게 한다.
4. 내부정보를 보호한다.
5. 화면의 보기 좋은 요소를 다듬는다.

## 2. 반드시 읽고 따라야 할 문서

구현 전과 기능 변경 전에는 아래 문서를 확인한다.

| 문서                                                | 사용 목적                                            |
| --------------------------------------------------- | ---------------------------------------------------- |
| `docs/TASK.md`                                      | 현재 구현 순서, 단계별 테스트, 완료 기록             |
| `docs/current_implementation_status.md`             | 실제 구현 완료 범위, 자동 확인 및 남은 브라우저 확인 |
| `docs/real_estate_listing_prd_v1.md`                | 제품 목적, 범위, 핵심 기능                           |
| `docs/real_estate_data_dictionary_v1.md`            | 어떤 정보를 어디에 저장할지                          |
| `docs/real_estate_listing_workflow_spec_v1.md`      | 신규 등록·재등록·수정의 업무 흐름                    |
| `docs/real_estate_screen_spec_wireframe_v1.md`      | 화면 구성과 사용성 기준                              |
| `docs/real_estate_test_acceptance_v1.md`            | 통과해야 할 테스트 시나리오                          |
| `docs/real_estate_operations_backup_handover_v1.md` | 운영, 백업, 복구, 인수인계 기준                      |
| `docs/tailscale_main_pc_connection_plan.md`         | 메인 PC의 Tailscale Serve 연결 순서와 접근 제한 기준 |

문서끼리 내용이 다르게 해석될 때는 다음 순서로 판단한다.

1. 데이터 안전과 내부정보 보호에 더 엄격한 기준
2. 데이터 항목 정의서
3. 업무 흐름서와 테스트 기준서
4. PRD와 화면 명세
5. `TASK.md`의 현재 단계 계획

판단이 어려우면 임의로 구현하지 말고, `TASK.md`의 결정 기록에 선택지가 필요한 이유를 남긴다.

## 3. 1차 버전의 고정 범위

### 구현 대상

- 건물, 호실, 매물 회차의 등록·조회·수정
- 기존 건물 검색과 새 호실 등록
- 같은 호실의 새 매물 회차 등록과 과거 이력 조회
- 현재 매물 목록, 검색, 필터, 오늘의 확인 업무
- 건물·호실의 고정정보 관리
- 사진 보유 여부와 촬영 필요 상태 관리(사진 파일·경로·링크는 관리하지 않음)
- 매물 회차에 연결한 단기·일반 계약 기간과 상태 관리
- 매물 회차에 연결한 간단한 상담 이력과 다음 연락일 관리
- 매물 회차별 현재 광고 채널·상태 관리
- 매물 회차별 보유처 관리
- 오늘 할 일과 계약 일정 조회
- 매물·계약·상담·오늘 할 일 엑셀 내보내기
- SQLite 데이터 저장, 백업, 복구 확인

### 구현하지 않는 대상

- 기존 `구재현` 엑셀 자동 가져오기 또는 일괄 이전
- 고객 로그인, 공개 매물 홈페이지, 외부 인터넷 공개
- 방문 예약, 자동 문자 발송, 주민번호 등 민감 개인정보, 계약금·잔금·결제의 상세 관리
- 사진·계약서·신분증 파일을 데이터 파일에 직접 저장
- 복잡한 권한관리, 광고 자동 등록·외부 광고 연동, 자동 알림 기능
- 같은 매물을 여러 사람이 동시에 편집하는 기능
- 장식성 카드, 그래프, 애니메이션 중심 화면

범위 밖 기능 요청은 기존 흐름을 깨지 않는 별도 제안으로 정리하고, 승인 전에는 구현하지 않는다.

## 4. 데이터 구축 규칙

### 4.1 정보의 자리

데이터는 반드시 아래 구조로 저장한다.

```text
건물 1개
└─ 호실 여러 개
   └─ 매물 회차 여러 개
```

- **건물**에는 주소, 주차, 엘리베이터, 공동현관 비밀번호처럼 여러 호실이 함께 쓰는 정보를 저장한다.
- **호실**에는 호수, 구조, 방향, 옵션, 방문 방법처럼 대체로 변하지 않는 정보를 저장한다.
- **매물 회차**에는 이번 상태, 보증금, 월세, 관리비, 입주일, 사진 상태, 사진 보유 여부, 현장 상태처럼 매번 달라질 수 있는 정보를 저장한다.
- 계약·상담·광고 현황은 건물이나 호실이 아닌 **매물 회차**에 연결한다. 광고 현황은 채널별 현재 상태만 관리하며 이력을 누적하지 않는다. 상담 기록에는 팔로업을 위해 고객 이름과 연락처를 저장할 수 있으나, 결제·계약서 파일·주민번호는 관리하지 않는다.

### 4.2 이력 보존

- 기존 호실을 다시 확인하거나 조건이 바뀌면 **같은 호실의 최신 매물 기록을 수정**한다. 일반적인 가격·상태·입주일 변경 때문에 새 매물 회차를 만들지 않는다.
- 기존 데이터에 이미 여러 매물 회차가 있더라도 자동으로 삭제하지 않는다. 새 입력은 현재 운영 중인 회차를 우선 수정하고, 현재 회차가 없으면 가장 최근 회차를 최신 상태로 다시 사용한다.
- 최신 매물 기록을 수정해도 그 매물에 연결된 계약·상담·광고 현황 기록은 유지한다.
- 매물이 사라졌다는 이유만으로 계약 완료로 바꾸지 않는다. 실제 확인 전에는 `확인 필요` 또는 `보류`를 사용한다.
- 잘못 등록한 매물은 종료 사유 `정보 오류`로 남기거나, 사용자가 명시적으로 확인한 경우 완전 삭제할 수 있다. 완전 삭제 시 연결된 계약·상담·광고 현황 기록도 함께 삭제됨을 안내한다.
- 계약·상담·광고 현황은 각각의 관리 화면에서 사용자가 명시적으로 확인한 경우 완전 삭제할 수 있다. 이 기록을 삭제해도 매물 기록은 유지한다.
- 건물은 기본 화면에서 완전 삭제하지 않는다. 호실은 잘못 만든 경우에만 사용자의 명시적 확인 후 완전 삭제할 수 있으며, 연결된 매물·계약·상담·광고 현황 기록도 함께 삭제됨을 먼저 보여 준다. 건물 정보는 남긴다.

### 4.3 입력과 값 규칙

- 필수값: 건물명, 지번, 호수, 매물 상태, 보증금, 월세, 입주 가능 유형이다.
- 금액은 데이터에 숫자로 저장하고 화면에서는 항상 `(만원)`을 표시한다.
- 가격을 모르면 `0`으로 저장하지 않는다. `확인 필요`와 확인 메모로 남긴다.
- `날짜 지정` 입주를 선택하면 입주일은 반드시 입력하게 한다.
- 모르는 선택정보는 억지로 추측하지 않고 빈값 또는 `확인 필요`를 쓴다.
- 검색·필터가 필요한 값은 자유 메모에만 넣지 않고 별도 항목으로 저장한다.
- 자동 기록: 내부 번호, 생성일시, 수정일시, 활성 여부를 유지한다.

### 4.4 중복 방지

- `건물명 + 지번`이 같으면 기존 건물 후보를 먼저 보여 준다.
- 건물명만 같고 지번이 다르면 절대 자동으로 합치지 않는다.
- 같은 건물 안에서 같은 호수는 하나만 존재해야 한다.
- `302`와 `302호`는 같은 호실로 비교한다.
- 새 건물·호실·첫 매물을 함께 저장할 때는 모두 성공하거나 모두 취소되어야 한다. 일부만 남으면 안 된다.

## 5. 화면 구축 규칙

### 5.0 코드 파일 역할 분리

- `app.py`는 프로젝트 최상위 폴더의 프로그램 입구로만 사용한다. 공통 화면 모양, 상단 메뉴, 현재 선택한 화면의 연결만 담당한다.
- 등록·재등록·수정 기능은 `ui/`와 `services/` 폴더의 별도 파일로 분리한다. `app.py`에 입력칸, 입력 검사, 저장 규칙을 길게 넣지 않는다.
- 실제 SQLite 파일은 `storage/real_estate.db`에서 관리한다. 공통 연결·표 구조·기존 데이터 호환은 `storage/database.py`에 두고, 업무별 읽기·저장 코드는 `storage/`의 기능별 저장소 파일에 나눈다. 화면 코드에서 SQLite 명령을 직접 실행하지 않는다.
- 화면에서 받은 입력을 검사하고 저장 순서를 정하는 코드는 `services/` 폴더의 기능별 파일에 둔다.
- 화면별 권장 분리 예시는 다음과 같다.

| 역할             | 권장 파일                                                    | 하는 일                              |
| ---------------- | ------------------------------------------------------------ | ------------------------------------ |
| 프로그램 입구    | `app.py`                                                     | 메뉴, 공통 모양, 화면 전환           |
| 등록·수정 화면   | `ui/listing_form.py`                                         | 사용자에게 입력칸을 보여 줌          |
| 등록·수정 규칙   | `services/listing_service.py`                                | 입력 검사, 저장 순서, 오류 문구 결정 |
| 오늘 할 일       | `ui/today_tasks.py`                                          | 매물·계약·상담의 당일·지연 업무 계산 |
| 매물 현황        | `ui/dashboard.py`                                            | 목록·필터·현재 매물 엑셀             |
| 건물·호실 관리   | `ui/building_management.py`                                  | 고정정보와 이력 화면                 |
| 계약·상담·광고 관리 | `ui/contract_management.py`, `ui/consultation_management.py`, `ui/advertisement_management.py` | 매물 회차별 계약·상담·광고 현황 관리 |
| 데이터 저장      | `storage/database.py`, `storage/*_repository.py`             | 공통 연결·표 구조와 업무별 읽기·저장 |
| 공통 선택값·검사 | 필요 시 별도 파일                                            | 상태값, 선택 목록, 공통 검사         |

- 파일을 나누는 목적은 복잡하게 보이게 하는 것이 아니라, 한 기능을 고쳐도 다른 기능이 망가지지 않게 하는 것이다.
- 기능이 아직 없으면 빈 기능 파일을 미리 만들지 않는다. 해당 단계에서 실제 기능을 만들 때 역할에 맞는 폴더에 추가한다.

### 5.1 공통 화면 원칙

- 기본 화면은 표 중심으로 만들고, 한 화면에서 충분한 행을 읽을 수 있게 한다.
- 흰색 또는 낮은 대비의 단순한 화면을 사용한다. 카드와 장식은 최소화한다.
- 저장, 수정, 새 회차 생성 버튼은 문구와 위치만 보고도 구분되어야 한다.
- 기본 입력칸은 최소화하고, 드물게 쓰는 항목은 `상세정보` 안에 접어 둔다.
- 입력 오류는 해당 칸 가까이에 쉬운 말로 표시한다.
- 저장 실패 시 사용자가 입력한 다른 값은 지우지 않는다.
- 화면 이동 전 저장하지 않은 변경이 있으면 경고한다.
- 빈 데이터 상태에서는 빈 표가 아닌 첫 매물 등록 안내와 버튼을 표시한다.

### 5.2 필수 화면

1. **오늘 할 일**
   - 매물·계약·상담의 날짜 업무를 `오늘`과 `지연`으로 구분하고, 매물의 날짜 없는 확인 항목은 `상시 확인 필요`로 표시한다.
   - 계약 진행 시작일과 건물·광고 업무는 오늘 할 일에 넣지 않는다.
   - 선택한 기준일의 업무를 개인정보 없이 엑셀로 내보낼 수 있다.

2. **매물 현황 리스트**
   - 첫 화면으로 사용한다.
   - 오늘 새 접수, 퇴실 예정, 재확인 필요, 사진 촬영 필요, 공실을 간단히 보여 준다.
   - 건물명·지번·호수 검색과 상태·룸 형태·사진·확인 업무 필터를 제공한다.

- 기본 표에서 상태, 건물명, 호수, 룸 형태, 보증금, 월세, 관리비, 입주 가능, 사진 상태, 사진 보유 여부, 재확인일, 메모를 우선 보인다.
- 행 선택 시 같은 화면 아래에서 상세를 펼친다.

3. **매물 등록·수정**
   - 건물 검색 → 호실 선택 → 이번 매물 입력 → 저장 순서를 유지한다.
   - 신규 등록, 기존 건물의 새 호실, 기존 호실 최신 정보 수정 상황을 명확히 구분한다.
   - 버튼 문구는 반드시 상황에 맞게 `건물·호실·첫 매물 등록`, `현재 매물 등록`, `최신 정보 저장`처럼 쓴다.
   - 저장 전에는 건물·지번·호수·가격 요약을 보여 준다.

4. **건물·호실 관리**
   - 건물 공통정보, 호실 목록, 호실 고정정보, 매물 이력을 한 흐름으로 확인한다.
   - 건물 기본정보 수정과 호실 기본정보 수정을 분리한다.
   - 이번 매물에만 다른 옵션은 호실 기본 옵션을 바꾸지 않고 매물 회차 메모에 저장한다.

5. **계약관리 / 상담관리 / 광고관리**
   - 계약과 상담 기록은 모두 건물·호실이 아닌 해당 매물 회차에 연결한다.
   - 계약은 일반·단기 계약의 기간·상태를 누적 관리하고, 계약자 연락처·계약금·잔금은 상세에서만 다룬다. 정식 계약·추가 계약금 수령·잔금·임대차 종료일은 계약 일정으로 확인한다. 납부·결제 관리와 계약서 파일은 만들지 않는다.
   - 상담은 고객 이름·연락처, 상담 이력, 다음 연락일을 누적 관리하며 고객 연락처는 목록에 기본 표시하지 않는다.
   - 광고관리는 당근·직방·네이버 또는 직접 입력 채널을 한 매물 회차에 복수로 연결하고, 현재 상태만 빠르게 수정한다.
   - 매물 보유처는 매물 회차에 기록하며, 크린주택관리·삼성주택관리·한빛주택관리·국제주택관리·개인매물 또는 직접입력을 사용한다.

### 5.3 오늘 할 일 계산 규칙

별도의 복잡한 할 일 목록 대신 아래 조건으로 자동 표시한다.

- 재확인 예정일·퇴실 예정일이 기준일과 같음 → 오늘
- 재확인 예정일·퇴실 예정일이 기준일보다 과거 → 지연
- 정식 계약일·계약금 추가 수령 예정일·잔금 예정일·임대차 종료일이 기준일과 같음 → 오늘, 과거 → 지연
- 상담 다음 연락일이 기준일과 같음 → 오늘, 과거 → 지연
- 사진 보유 여부가 없음 → 사진 촬영 필요
- 사진 보유 여부가 확인 필요 → 사진 확인 필요
- 청소·도배·수리가 필요 또는 진행 중 → 현장 상태 확인 필요
- 입주 가능 유형이 확인 필요 → 입주 가능일 확인 필요
- 매물 상태가 확인 필요 → 매물 상태 확인 필요
- 계약 진행 시작일은 가계약·구두 합의 이력으로만 관리하며 오늘·지연 업무에 넣지 않는다.
- 건물 정보 재확인과 광고 현황은 오늘 할 일에 넣지 않는다.

## 6. 내부정보·보안 규칙

다음은 내부정보다.

- 공동현관 비밀번호
- 방문 비밀번호
- 열쇠 위치
- 임대인·세입자 연락처
- 상담 고객의 이름과 연락처
- 내부 메모와 내부 확인사항

반드시 지킬 것:

- 기본 매물 목록과 오늘의 현황에는 내부정보를 표시하지 않는다.
- 상세 화면에서 `비밀번호 보기` 같은 명시적 동작을 했을 때만 볼 수 있게 한다.
- 외부 공유용 엑셀·공개 링크에는 내부정보를 절대 포함하지 않는다.
- 매물 현황 리스트의 **내부 업무용 엑셀**에는 공동현관·방문 비밀번호와 내부 메모를 포함할 수 있다. 단, 임대인·세입자·계약자·상담 고객의 개인 연락처와 상담 고객 이름은 절대 포함하지 않으며, 이 파일을 외부에 공유하지 않는다.
- 광고 문구나 외부 공유 기능을 만들 경우에도 내부정보를 전달하지 않는다.
- 프로그램은 사무실 내부 네트워크에서만 사용한다. 외부 공개 주소, 포트 공개, 공개 링크를 만들지 않는다.
- 비밀번호가 들어 있을 수 있는 백업 파일이나 엑셀을 공개 링크로 공유하지 않는다.

## 7. 저장·운영·배포 규칙

- 실제 SQLite 데이터 파일은 지정된 사무실 운영 PC 한 곳에만 둔다.
- 사용자 PC는 데이터 파일을 직접 열거나 복사하지 않고, 브라우저로 운영 PC의 프로그램에 접속한다.
- 현재 운영 폴더는 `C:\realestate-main`이며, 실제 SQLite 원본은 `C:\realestate-main\storage\real_estate.db` 한 곳만 사용한다.
- 메인 PC에서는 프로젝트의 Python 3.12 가상환경으로 Streamlit을 `127.0.0.1:8501`에만 실행한다. `streamlit` 단독 명령 대신 `.\.venv\Scripts\python.exe -m streamlit` 형식으로 실행한다.
- 두 PC 운영은 메인 PC 한 대에서 프로그램과 데이터 파일을 실행하고, 허용된 두 사용자만 **Tailscale Serve** 전용 HTTPS 연결로 접속한다. 일반 인터넷 공개 기능, Tailscale Funnel, 공유기 포트 포워딩은 사용하지 않는다.
- Tailscale 접근은 개별 직원 계정·허가된 기기·접근정책으로 제한한다. 계정을 공유하지 않으며, 퇴사자 또는 교체 기기는 즉시 접근 목록에서 제거한다.
- SQLite 원본을 Google Drive, OneDrive, 네트워크 공유폴더에서 직접 실행하지 않는다.
- 사진 원본과 사진 보관 위치는 이 도구에서 관리하지 않는다. 프로그램에는 사진 보유 여부만 기록하며, 사진 파일·경로·링크 입력 기능을 만들지 않는다. `없음`은 사진 촬영 필요, `확인 필요`는 사진 확인 필요 업무로 표시한다.
- 프로그램 코드와 실제 데이터 파일은 분리한다. 코드 업데이트가 데이터 파일을 덮어쓰면 안 된다.
- 배포 전: 운영 폴더 전체와 데이터 파일을 백업하고, 프로그램을 정상 종료한 뒤 코드만 교체한다.
- 배포 후: 신규 등록, 재등록, 과거 이력 조회를 반드시 다시 확인한다.
- 변경일, 변경자, 변경 내용, 확인 결과를 기록한다.

## 8. 백업·복구 규칙

- 매물·계약·상담·광고 현황의 저장·수정·삭제 성공 뒤에는 SQLite 온라인 백업과 무결성 검사를 거쳐 OneDrive `Documents\매물관리백업\daily`에 그날의 사본을 갱신한다.
- 일일 자동 백업은 날짜별 파일 하나로 유지하고, 최근 10일을 초과한 파일은 새 백업에 성공한 뒤에만 정리한다. 성공·실패와 파일 크기는 `logs\backup.log`와 `logs\backup_status.json`에 기록한다.
- 주간: 프로그램 폴더와 데이터 전체를 백업하고 최근 8개를 보관한다.
- 월간: 월말 전체 백업을 최소 12개월 보관한다.
- 큰 변경 전: 변경 전·후 백업을 각각 1개 이상 남긴다.
- 백업 파일명에는 날짜와 시간을 넣는다.
- 백업은 원본 데이터와 다른 드라이브 또는 접근이 제한된 장소에 둔다.
- 백업을 만들 때 원본 SQLite 파일을 이동·삭제·덮어쓰지 않는다.
- 복구는 먼저 별도 검증 폴더에서 백업본을 열어 건물 수, 호실 수, 최근 매물, 과거 이력을 확인한다.
- 실제 복구 전에는 모든 사용자의 저장이 끝났는지 확인하고 프로그램을 종료한다.
- 복구 직전 원본은 별도 보관하며 최소 30일 유지한다.

### 현재 구현 상태 (2026-08-07)

- 1~9단계와 11~13단계, 계약 일정·오늘 할 일·광고관리의 코드 구현 및 기본 자동 확인을 마쳤다. 10단계의 전체 백업·복구·사무실 운영 검증은 사용자 요청으로 보류 중이다.
- 실제 브라우저에서 업무 순서대로 확인하는 작업은 각 완료 단계에 남아 있다. 코드·자동 확인 완료와 실제 화면 확인 완료를 혼동하지 않는다.
- 메인 PC와 서브 PC의 Tailscale 연동, 메인 PC의 `127.0.0.1:8501` Streamlit 실행, `RealEstate Streamlit` 예약 작업 및 `start_streamlit.bat` 등록은 마쳤다. 재부팅 뒤 자동 로그인·30초 지연 실행과 서브 PC HTTPS 접속은 실제 확인이 남아 있다.

## 9. 구현과 검증의 작업 방식

모든 기능은 아래 순서로 진행한다.

1. `docs/TASK.md`에서 현재 단계와 완료 기준을 확인한다.
2. 작은 단위로 구현한다.
3. 코드 수준의 기본 확인을 한다.
4. 브라우저에서 실제 업무 순서대로 직접 확인한다.
5. 관련 테스트 시나리오를 PASS / FAIL / BLOCKED로 기록한다.
6. 문제가 있으면 원인, 화면, 재현 방법을 남기고 수정한다.
7. 수정 후 해당 테스트와 영향받는 기존 테스트를 다시 실행한다.

### 최소 테스트 규칙

- 신규 등록: TC-001~006
- 중복 방지: TC-007~011
- 재등록: TC-012~017
- 수정·종료: TC-018~028
- 오늘의 현황: TC-029~035
- 화면 사용성: TC-036~043
- 내부정보 보호·엑셀: TC-044~050
- 저장·백업·복구: TC-051~056
- 사무실 2인 사용: TC-057~060

치명적 또는 높은 수준의 문제는 해결 전 다음 단계나 운영 배포로 넘어가지 않는다.

- **치명적**: 데이터가 사라짐·손상됨, 프로그램을 쓸 수 없음
- **높음**: 신규 등록 불가, 재등록 시 과거값 덮어쓰기 등 핵심 업무 불가

## 10. 변경 시 반드시 확인할 것

- 데이터 항목이나 상태값을 바꾸기 전: 데이터 항목 정의서를 먼저 수정한다.
- 화면 흐름을 바꾸기 전: 화면 명세와 업무 흐름서를 함께 수정한다.
- 기능을 추가하거나 고친 뒤: 관련 테스트 시나리오를 갱신하고 다시 통과시킨다.
- 데이터 구조를 바꾸기 전: 전체 백업을 만든다.
- 모든 의미 있는 변경 뒤: 최소 TC-001, TC-014, TC-018, TC-025, TC-051, TC-054를 다시 실행한다.
- 실제 사용 중 새 요구가 생기면 먼저 건물·호실·매물 회차 중 어디에 속하는지 판단한다.

## 11. 절대 하지 않을 일

- 과거 매물 회차의 가격·상태·입주일을 재등록 때문에 덮어쓰기
- 모르는 가격을 0으로 저장
- 매물이 보이지 않는다는 이유만으로 계약 완료 처리
- 운영 중인 데이터 파일을 개발용 데이터 파일로 덮어쓰기
- 백업 확인 없이 원본 데이터 삭제
- 사용자 PC마다 SQLite 파일을 복사해 따로 운영
- SQLite 원본을 클라우드 동기화 폴더에서 직접 실행
- 내부 비밀번호나 내부 메모를 기본 표·일반 엑셀·공개 링크에 노출
- 인터넷 공개 주소나 외부 접속용 포트를 설정
- 문서에 없는 기능을 편의상 데이터 구조에 먼저 넣기

## 12. 빌드 완료 기준

아래 조건을 모두 만족해야 1차 버전 완료로 판단한다.

- 신규 건물·호실·첫 매물 등록이 정상 동작한다.
- 기존 호실을 선택해 최신 매물 정보만 바로 수정할 수 있다.
- 최신 정보 수정 뒤에도 연결된 계약·상담 기록이 유지된다.
- 오늘의 현황에서 재확인·사진 촬영 필요 매물을 찾을 수 있다.
- 비밀번호가 기본 목록과 일반 엑셀에 보이지 않는다.
- 프로그램 재시작 후 데이터가 유지된다.
- 백업이 생성되고, 백업본으로 조회·복구 확인을 마쳤다.
- 사무실 사용자 PC 두 대에서 같은 매물 목록을 본다.
- TC-001~060 중 적용 대상이 모두 PASS다.
- 치명적·높음 등급 문제가 0건이다.
- 실제 사용자가 별도 설명 없이 대성빌 302호 신규 등록과 재등록을 끝낼 수 있다.
